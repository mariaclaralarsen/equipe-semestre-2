import numpy as np
def resolver_sistema_linear(A, b):

 x= np.linalg.solve(A, b)
 return x

# Exemplo de uso 
A = np.array([[2, 1], [1, -1]])
b = np.array([5, 1])
x = resolver_sistema_linear(A, b)
print(x)
